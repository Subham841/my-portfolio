{
  "entities": {
    "Project": {
      "title": "Project",
      "type": "object",
      "description": "Represents a project in the portfolio.",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "technologies": { "type": "array", "items": { "type": "string" } },
        "role": { "type": "string" },
        "imageUrl": { "type": "string", "format": "uri" }
      },
      "required": ["title", "description", "technologies", "role", "imageUrl"]
    },
    "Contact": {
      "title": "Contact Submission",
      "type": "object",
      "description": "Represents a message submitted via the contact form.",
      "properties": {
        "name": { "type": "string" },
        "mobile": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "message": { "type": "string" },
        "submittedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "message", "submittedAt"]
    },
    "Settings": {
        "title": "Site Settings",
        "type": "object",
        "description": "Global settings for the portfolio site.",
        "properties": {
            "profileImageUrl": { "type": "string", "format": "uri" }
        }
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/projects/{projectId}": {
      "schema": { "$ref": "#/entities/Project" },
      "description": "Stores individual project details. Publicly readable."
    },
    "/contacts/{contactId}": {
      "schema": { "$ref": "#/entities/Contact" },
      "description": "Stores contact form submissions. Writable by anyone, but readable only by an authenticated admin."
    },
    "/settings/{settingId}": {
        "schema": { "$ref": "#/entities/Settings" },
        "description": "Stores global site settings, like the profile image URL. Publicly readable."
    }
  }
}
